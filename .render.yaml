services:
  - type: web
    name: traffic-control
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend.traffic_control:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.7